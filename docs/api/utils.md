# Utilities

The `utils` module includes various utilities that are used at multiple points in the package,
but which can also be useful as standalone components in interpreting data or analysing
ORGANIC's outputs.

:::organic.utils
