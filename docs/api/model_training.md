# Model training

The `model_training` module is used to train WGANs on training data, which after
extensive intitial training can be used for image reconstructions with ORGANIC.

:::organic.model_training
